# Storage mode: "demo" (localStorage, default) or "production" (PostgreSQL)
NEXT_PUBLIC_STORAGE_MODE=demo

# Only required when NEXT_PUBLIC_STORAGE_MODE=production
DATABASE_URL=postgresql://user:password@localhost:5432/doit
